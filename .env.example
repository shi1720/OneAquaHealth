PORT=8787
DATABASE_PATH=./data/rill.sqlite
# Set to the public HTTPS origin in production.
APP_ORIGIN=http://localhost:5173
# Optional remote SQLite-compatible libSQL storage; keep tokens out of Git.
# TURSO_DATABASE_URL=libsql://your-database.turso.io
# TURSO_AUTH_TOKEN=
# REQUIRE_REMOTE_DATABASE=true
# No external model is called. No inference key is required.
